# 1. Хто QA / Хто Core

### QA частиною займався Бичко Арсеній, Core частиною - Пошитнюк Дмитро

# 2. Як запускати тести

### Для того щоб запустити перевірку тестів, відкрийте папку "Lab2", після чого натисніть вгорі на кнопку "Terminal" та оберіть "New Terminal" (для VS Code).

### Натисніть вгорі на "View" - "Terminal" (для Visual Studio)

###  Після цього введіть в термінал команду

```
dotnet test
```


# 3. Як перевірити: List через enumerator, Sort default, Sort альтернативне, Stats

### 1.	List через IEnumerator
- В меню оберіть пункт 2 (перегляд списку).
- Після звичайного виводу списку відобразиться блок 
```
--- Перебір через enumerator ---
```

який використовує:

```
var it = collection.GetEnumerator(); while (it.MoveNext()) { ... }.
```

### 2.	Sort default (IComparable)
- В меню оберіть пункт 6 **(Статистика)**.
- У блоці Сортування за ім'ям **(IComparable)** відображається результат сортування за природним порядком (метод **CompareTo**).
### 3.	Sort альтернативне (IComparer)
- В меню оберіть пункт 4 **(Лідерборд)**.
- Список сортується альтернативним компаратором **PlayerScoreComparer** (сортування за очками).
### 4.	Stats
- В меню оберіть пункт 6.
- Буде показано кількість гравців, суму балів, середній бал та максимум.
